/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { Dropdown.directiveDirective } from './dropdown.directive.directive';

describe('Directive: Dropdown.directive', () => {
  it('should create an instance', () => {
    const directive = new Dropdown.directiveDirective();
    expect(directive).toBeTruthy();
  });
});
